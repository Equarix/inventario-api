version: '3.8'

services:
  inventario-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inventario_api
    env_file:
      - .env
    ports:
      - "8080:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
